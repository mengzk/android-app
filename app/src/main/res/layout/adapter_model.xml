<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/dimen_12"
    android:background="@drawable/bg_msg"
    android:id="@+id/model_item_box">

    <TextView
        android:id="@+id/model_item_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:textColor="@color/title"
        android:textSize="@dimen/size_16"
        android:text="名称"/>

    <TextView
        android:id="@+id/model_item_desc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/model_item_name"
        app:layout_constraintStart_toStartOf="@id/model_item_name"
        android:textColor="@color/title"
        android:textSize="@dimen/size_16"
        android:text="描述"/>

</androidx.constraintlayout.widget.ConstraintLayout>